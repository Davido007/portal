'use strict';

angular.module('angularBootstrapGruntBowerApp')
    .controller('MainController', function ($scope, $rootScope, $http, urls, $cookies, AuthSharedService) {
        $scope.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
console.log("aaa")
    });
